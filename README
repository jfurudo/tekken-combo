Tekken Combo
=================================

## Tasks
- define a table `combo`
```
start: int(move.id)
recipe: List[int(move.id)]
author: user_id
```
- refactor the table `move`
-- hit -> hit_min, hit_max ,guard, ch and so on.

This file will be packaged with your application, when using `activator dist`.
